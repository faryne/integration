# 書籤功能

## 目的
- 使用者可以在每個故事中的每個章節任一行加上書籤。而且使用者可以根據書籤快速跳轉到該行。
- 使用者在一個章節中可以加上多個書籤
- 使用者可以透過點擊書籤列表中快速到達每個章節的每個段落。

## 工作項
- 故事頁每一行左側加上一個書籤符號，按下後即可加入該使用者的書籤
- 再按一次從書籤中移除
- 加入和移動的動作為非同步，避免即時且頻繁地操作
- 書籤會與目錄放在一起，讓使用者決定切換章節目錄或是書籤
- 書籤的移除使用 hard delete 